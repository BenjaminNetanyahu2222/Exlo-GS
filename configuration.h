#pragma once

namespace Configuration {
	static inline bool bLateGame = true; 
	static inline bool bRespawn = false; //WIP, do not use unless testing
	static inline bool bIs1v1 = false; //WIP, do not use unless testing
};